pub struct Profile {
    pub assets: isize,
    pub liabilities: f32,
    pub Equity: f32,
}

struct Commodity {
    pub currency: u16,
    pub name: String,
    pub note: String,
    pub way: String,
    pub tags: Vec<String>,
    pub number: f32,
    pub money: f32,
}

/*
CNY 156
JPY 392
 */
